version: 0.2

phases:
  # install:
    # runtime-versions:
    #   golang: 1.21
  pre_build:
    commands:
      - echo Running pre-build steps...
  build:
    commands:
      - echo Build started on `date`
      - echo Building the docker image...
      - docker build -t test:latest -f ./build/Dockerfile --no-cache . 
    on-failure: ABORT
  post_build:
    commands:
      - echo Build completed on `date`

# artifacts:
#   files:
#     - '.'